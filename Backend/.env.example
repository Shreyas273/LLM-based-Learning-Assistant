# Example environment for local Backend development.
# Copy to Backend/.env and fill in real values (never commit .env).

GEMINI_API_KEY=
LLM_PROVIDER=gemini
MONGO_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?appName=ai-learning-cluster
CORS_ORIGINS=http://localhost:5173
